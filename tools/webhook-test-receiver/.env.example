# Optional host-user overrides for the read-only Cloudflare credential mount.
LOCAL_UID=1000
LOCAL_GID=1000

# Receiver runtime configuration.
PORT=8787
DATA_DIR=/app/data
SECRETS_DIR=/run/secrets/webhooks
MAX_BODY_BYTES=131072
TIMESTAMP_TOLERANCE_SECONDS=300
