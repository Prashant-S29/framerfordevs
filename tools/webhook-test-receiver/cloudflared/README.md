# Cloudflare authority directory

Keep the locally managed tunnel credential JSON and `config.yml` here. Both are ignored by Git and Docker build contexts and mounted read-only at runtime.

`config.yml.example` is the only committed file in this directory.
